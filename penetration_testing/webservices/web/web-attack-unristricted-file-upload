# ------------------------------- start overview ------------------------------- # 
    ==> Quick view: The consequences of unrestricted file upload can vary, including complete system takeover, 
                    an overloaded file system or database, forwarding attacks to back-end systems, and simple defacement. 
                    It depends on what the application does with the uploaded file and especially where it is stored.

    ==> unrestricted file upload types:
        - unrestricted type.
        - unrestricted size.
        - unrestricted frequency.
        - unrestricted real time scan.
# -------------------------------   end overview ------------------------------- # 